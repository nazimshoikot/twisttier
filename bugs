take a look at the /api/update/{username}. It doesn't seem to update anything even after passing all the data defined in communication requirements
Bill gates needs to fix azure dev ops
Dark mode is broken yo